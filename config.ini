# Maximum number of concurrent TCP connections.
max_connections = 100

# TTL eviction daemon sampling interval in milliseconds.
# Lower values evict expired keys faster at the cost of more CPU usage.
hz = 100
